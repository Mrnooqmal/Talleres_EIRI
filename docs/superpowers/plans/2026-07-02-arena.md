# EIRI Arena Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Pantalla de competencia en vivo en `/arena` (selección de partido → presentación VS → combate con timer y 3 corazones → victoria con confirmación que avanza el bracket público, incluyendo 3er lugar).

**Architecture:** Página server-rendered integrada al patrón del repo (como `/admin`): ruta Express protegida, template nunjucks, vanilla JS + CSS propios. Estado del combate en el navegador con respaldo en `localStorage`; persistencia solo al confirmar vía endpoint nuevo `PUT /api/admin/bracket/match`. El partido de 3er lugar se agrega al modelo `bracket_data` y se alimenta solo con los perdedores de semifinales.

**Tech Stack:** Express 4 + nunjucks + SQLite (`lib/db`), vanilla JS, Web Audio API, lucide icons (ya incluido). Sin dependencias nuevas.

**Spec:** `docs/superpowers/specs/2026-07-02-arena-design.md`

## Global Constraints

- Sin dependencias npm nuevas.
- Sin framework de tests en el repo: la verificación es curl contra el server local (`npm run dev`, puerto 3000) + checklist manual en navegador.
- Login local de desarrollo: `admin` / `eiri2026` (seed en server.js). Cookie: `curl -s -c /tmp/eiri.jar -X POST localhost:3000/api/admin/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"eiri2026"}'`.
- Estilo de código: seguir el existente — server.js sin punto y coma final, JS de static/ con punto y coma; comentarios en español.
- Commits frecuentes con mensajes en español, prefijos `feat:`/`fix:`, y trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Formato de match en `bracket_data`: `{ a, b, scoreA, scoreB, winner }` con `a`/`b` = team ids o null, scores strings, winner `'a'|'b'|null`. El `third` usa el mismo formato.
- Los scores confirmados desde la arena son los corazones restantes (strings, ej. `"3"` y `"1"`).

---

### Task 1: Server — audio en uploads + columna `anthem` en teams

**Files:**
- Modify: `server.js:24` (ALLOWED_FILES), `server.js:232-236` (migrations), `server.js:814-836` (POST/PUT teams)

**Interfaces:**
- Produces: columna `teams.anthem` (TEXT, URL de audio); `POST/PUT /api/admin/teams` aceptan `anthem`; `/api/teams` y `/api/admin/teams` ya la devuelven (usan `SELECT *`).

- [ ] **Step 1: Permitir archivos de audio en uploads**

En `server.js:24` reemplazar:

```js
const ALLOWED_FILES = /\.(png|jpg|jpeg|gif|webp|svg|pdf|ino|c|cpp|h|hpp|py|js|ts|json|txt|md|csv)$/i
```

por:

```js
const ALLOWED_FILES = /\.(png|jpg|jpeg|gif|webp|svg|pdf|ino|c|cpp|h|hpp|py|js|ts|json|txt|md|csv|mp3|m4a|ogg|wav)$/i
```

- [ ] **Step 2: Migración de la columna**

Junto a las otras migraciones (`server.js:232-236`) agregar:

```js
  try { await db.run("ALTER TABLE teams ADD COLUMN anthem TEXT DEFAULT ''") } catch {}
```

- [ ] **Step 3: Aceptar `anthem` en create/update de equipos**

En `POST /api/admin/teams` cambiar la destructuración y el INSERT:

```js
  const { name, logo = '', anthem = '' } = req.body
  ...
    const r = await db.run('INSERT INTO teams (name, logo, anthem) VALUES (?,?,?)', name.trim(), logo, anthem)
```

En `PUT /api/admin/teams/:id`:

```js
  const { name, logo = '', anthem = '' } = req.body
  ...
    await db.run('UPDATE teams SET name=?, logo=?, anthem=? WHERE id=?', name.trim(), logo, anthem, req.params.id)
```

- [ ] **Step 4: Verificar con curl**

```bash
npm run dev &   # dejar corriendo
sleep 2
curl -s -c /tmp/eiri.jar -X POST localhost:3000/api/admin/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"eiri2026"}'
curl -s -b /tmp/eiri.jar -X POST localhost:3000/api/admin/teams -H 'Content-Type: application/json' -d '{"name":"__test_anthem","anthem":"http://x/y.mp3"}'
curl -s localhost:3000/api/teams | grep -o '"anthem":"http://x/y.mp3"'
```

Expected: la última línea imprime `"anthem":"http://x/y.mp3"`. Luego borrar el equipo de prueba (`DELETE /api/admin/teams/:id` con el id devuelto).

- [ ] **Step 5: Commit**

```bash
git add server.js
git commit -m "feat: columna anthem en equipos y audio permitido en uploads"
```

---

### Task 2: Server — partido de 3er lugar (`third`) en bracket_data

**Files:**
- Modify: `server.js:278` (seed), `server.js:365-415` (helpers bracket), `server.js:847-860` (getBracketPayload)
- Modify: `static/js/admin.js:1884-1889` y `static/js/admin.js:2081-2086` (passthrough de `third` al guardar desde el admin)

**Interfaces:**
- Produces: `GET /api/bracket` devuelve `third: { a, b, scoreA, scoreB, winner }` (a/b poblados automáticamente con los perdedores de semifinales) y `teams` incluye `anthem`. `emptyThirdMatch()` disponible en server.js.

- [ ] **Step 1: Helper y resolución**

Después de `emptyBracketRounds` en server.js agregar:

```js
function emptyThirdMatch() {
  return { a: null, b: null, scoreA: '', scoreB: '', winner: null }
}
```

En `resolveBracket`, antes del `return bracket`:

```js
  // 3er lugar: los perdedores de las semifinales (penúltima ronda)
  const semis = rounds.length >= 2 ? rounds[rounds.length - 2] : null
  if (semis && semis.length === 2) {
    if (!bracket.third) bracket.third = emptyThirdMatch()
    const loser = (m) => m.winner === 'a' ? m.b : m.winner === 'b' ? m.a : null
    bracket.third.a = loser(semis[0])
    bracket.third.b = loser(semis[1])
  }
```

- [ ] **Step 2: Sanitización y seed**

En `sanitizeBracket`, reemplazar el `return resolveBracket({ size, rounds: base })` por:

```js
  // El 3er lugar solo acepta scores/winner; los equipos se propagan desde las semis.
  const srcT = raw?.third || {}
  const third = emptyThirdMatch()
  third.scoreA = String(srcT.scoreA ?? '').slice(0, 6)
  third.scoreB = String(srcT.scoreB ?? '').slice(0, 6)
  third.winner = srcT.winner === 'a' || srcT.winner === 'b' ? srcT.winner : null
  return resolveBracket({ size, rounds: base, third })
```

En el seed de config (`server.js:278`):

```js
    bracket_data: JSON.stringify({ size: 8, rounds: emptyBracketRounds(8), third: emptyThirdMatch() }),
```

- [ ] **Step 3: Payload con `third` y `anthem`**

En `getBracketPayload`:

```js
  if (!data.third) data.third = emptyThirdMatch()   // brackets guardados antes de este cambio
  const teams = await db.all('SELECT id, name, logo, anthem FROM teams ORDER BY id')
  const resolved = resolveBracket(data)
  return {
    title:    cfg.bracket_title || 'Llave del torneo',
    subtitle: cfg.bracket_subtitle || '',
    size:     data.size,
    rounds:   resolved.rounds,
    third:    resolved.third,
    teams,
  }
```

- [ ] **Step 4: El editor admin no debe pisar `third`**

En `static/js/admin.js` `loadBracketAdmin()` agregar `third` a `bkState`:

```js
    bkState = {
      size: data.size || 8,
      rounds: Array.isArray(data.rounds) && data.rounds.length ? data.rounds : bkEmptyRounds(data.size || 8),
      third: data.third || null,
      title: data.title || '',
      subtitle: data.subtitle || '',
    };
```

Y en el click de `saveBracketBtn` incluirlo en el PUT:

```js
    await api('PUT', '/api/admin/bracket', {
      size: bkState.size,
      rounds: bkState.rounds,
      third: bkState.third,
      title: document.getElementById('bk-title').value.trim(),
      subtitle: document.getElementById('bk-subtitle').value.trim(),
    });
```

- [ ] **Step 5: Verificar con curl**

Con el server corriendo:

```bash
curl -s localhost:3000/api/bracket | python3 -c "import json,sys; d=json.load(sys.stdin); print('third' in d, d['third'])"
```

Expected: `True {'a': None, 'b': None, ...}` (o con ids si las semis ya tienen ganador). Además: marcar ganadores en ambas semifinales vía el admin o `PUT /api/admin/bracket` y verificar que `third.a`/`third.b` quedan con los ids de los perdedores.

- [ ] **Step 6: Commit**

```bash
git add server.js static/js/admin.js
git commit -m "feat: partido de 3er lugar en el modelo del bracket"
```

---

### Task 3: Server — `PUT /api/admin/bracket/match` + ruta `/arena`

**Files:**
- Modify: `server.js` (junto a los endpoints de bracket, ~línea 862; ruta de página junto a `/admin`, ~línea 486)
- Create: `templates/arena.html`, `static/css/arena.css`, `static/js/arena.js` (esqueleto mínimo para que la ruta cargue)

**Interfaces:**
- Produces: `PUT /api/admin/bracket/match` con body `{ round, index, scoreA, scoreB, winner }` o `{ third: true, scoreA, scoreB, winner }` → actualiza solo ese partido, re-resuelve y guarda; responde `{ ok: true }`. `GET /arena` renderiza la página (redirect a `/admin/login` sin sesión).

- [ ] **Step 1: Endpoint de partido único**

Después de `PUT /api/admin/bracket` en server.js:

```js
// Actualiza un solo partido (o el 3er lugar) — lo usa la arena al confirmar un resultado.
app.put('/api/admin/bracket/match', requireAdmin, async (req, res) => {
  const cfg = await getConfig()
  let data
  try { data = JSON.parse(cfg.bracket_data || '{}') } catch { data = {} }
  if (!Array.isArray(data.rounds)) data = { size: 8, rounds: emptyBracketRounds(8) }
  if (!data.third) data.third = emptyThirdMatch()
  resolveBracket(data)  // asegura participantes propagados antes de validar

  const { scoreA = '', scoreB = '', winner } = req.body
  if (winner !== 'a' && winner !== 'b') return res.status(400).json({ error: 'winner debe ser a o b' })

  let m, label
  if (req.body.third) {
    m = data.third
    label = '3er lugar'
  } else {
    const r = parseInt(req.body.round, 10), i = parseInt(req.body.index, 10)
    m = data.rounds[r]?.[i]
    if (!m) return res.status(400).json({ error: 'Partido inexistente' })
    label = `ronda ${r} partido ${i}`
  }
  if (m.a == null || m.b == null) return res.status(400).json({ error: 'El partido aún no tiene ambos equipos' })

  m.scoreA = String(scoreA).slice(0, 6)
  m.scoreB = String(scoreB).slice(0, 6)
  m.winner = winner
  resolveBracket(data)
  await db.run('INSERT OR REPLACE INTO site_config (key, value) VALUES (?,?)', 'bracket_data', JSON.stringify(data))
  await log(req, 'update_bracket_match', `${label}: ${m.scoreA}-${m.scoreB} winner=${winner}`)
  res.json({ ok: true })
})
```

- [ ] **Step 2: Ruta de página + esqueleto**

Junto a las rutas de página (`/admin`):

```js
app.get('/arena', async (req, res) => {
  if (!req.auth) return res.redirect('/admin/login')
  res.render('arena.html')
})
```

`templates/arena.html` (esqueleto; las fases se llenan en Tasks 5-8):

```html
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EIRI Arena</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Chakra+Petch:wght@400;600;700&family=JetBrains+Mono:wght@400;700&family=Orbitron:wght@600;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/static/css/style.css?v=20260615">
    <link rel="stylesheet" href="/static/css/arena.css?v=1">
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>
</head>
<body class="arena-body">
    <main id="arena" class="arena"></main>
    <script src="/static/js/arena.js?v=1"></script>
</body>
</html>
```

`static/js/arena.js` inicial:

```js
/* EIRI Arena — pantalla de competencia en vivo */
'use strict';
document.getElementById('arena').innerHTML = '<p style="padding:2rem">Arena OK</p>';
```

`static/css/arena.css` inicial:

```css
/* EIRI Arena — estilos de la pantalla de competencia */
.arena-body { overflow: hidden; }
.arena { min-height: 100vh; display: flex; flex-direction: column; }
```

- [ ] **Step 3: Verificar**

```bash
curl -s -o /dev/null -w "%{http_code} %{redirect_url}\n" localhost:3000/arena              # sin cookie
curl -s -b /tmp/eiri.jar localhost:3000/arena | grep -c "EIRI Arena"                        # con cookie
curl -s -b /tmp/eiri.jar -X PUT localhost:3000/api/admin/bracket/match \
  -H 'Content-Type: application/json' -d '{"round":0,"index":0,"scoreA":"3","scoreB":"1","winner":"a"}'
curl -s localhost:3000/api/bracket | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['rounds'][0][0], d['rounds'][1][0]['a'])"
```

Expected: `302 .../admin/login`; `1`; `{"ok":true}`; el match 0 con winner `a` y su equipo propagado a la ronda siguiente. Revertir el resultado de prueba dejando `winner: null` vía `PUT /api/admin/bracket` (o restaurando el JSON previo).

- [ ] **Step 4: Commit**

```bash
git add server.js templates/arena.html static/css/arena.css static/js/arena.js
git commit -m "feat: endpoint de partido único y ruta /arena"
```

---

### Task 4: Home — 3er lugar en el bracket público

**Files:**
- Modify: `static/js/app.js:679-777` (loadBracket), `static/css/style.css` (sección bracket, ~línea 1442)

**Interfaces:**
- Consumes: `data.third` de `GET /api/bracket` (Task 2).

- [ ] **Step 1: Render del 3er lugar en el centro**

En `loadBracket()` de app.js, reemplazar la construcción de `center` para incluir la tarjeta del 3er lugar y la línea de bronce (después del div `.bk-champ`, dentro del mismo `.bk-center`):

```js
    const third = data.third || null;
    const thirdWinner = third
      ? (third.winner === 'a' ? teamsById[third.a] : third.winner === 'b' ? teamsById[third.b] : null)
      : null;
    const thirdBlock = third && (third.a != null || third.b != null) ? `
      <div class="bk-third">
        <div class="bk-third-title"><i data-lucide="medal"></i> 3er lugar</div>
        ${matchCard(third)}
        ${thirdWinner ? `<div class="bk-third-winner">🥉 ${escapeHtml(thirdWinner.name)}</div>` : ''}
      </div>` : '';
```

y agregar `${thirdBlock}` al final del template de `center`, tras el cierre del div `.bk-champ`.

- [ ] **Step 2: Estilos**

En style.css, tras los estilos de `.bk-champ-name--tbd`:

```css
/* 3er lugar bajo el campeón */
.bk-third { margin-top: 1rem; padding-top: 0.8rem; border-top: 1px dashed var(--border); }
.bk-third-title {
  display: flex; align-items: center; justify-content: center; gap: 0.35rem;
  font-family: var(--f-mono); font-size: 0.6rem; letter-spacing: 0.2em;
  text-transform: uppercase; color: #cd7f32; margin-bottom: 0.5rem;
}
.bk-third-title svg { width: 13px; height: 13px; }
.bk-third .bk-card { border-color: rgba(205,127,50,0.35); }
.bk-third-winner { text-align: center; font-size: 0.78rem; color: #cd7f32; font-weight: 600; margin-top: 0.5rem; }
```

Nota: `.bk-champ` es `position: absolute; bottom: 0` — al agregar `.bk-third` dentro de `.bk-center` (position relative, flex column) verificar visualmente que no se sobrepongan; si chocan, mover `.bk-third` a `bottom: -X` o convertir `.bk-champ` a flujo normal con `margin-top: auto`. Decidirlo mirando el render real.

- [ ] **Step 3: Verificar en navegador**

Abrir `localhost:3000/#bracket`: con semis sin ganador el bloque de 3er lugar no aparece (third.a/b null); marcar ganadores en ambas semis desde el admin y verificar que aparece la tarjeta con los dos perdedores, estilos solo-logo con hover intactos.

- [ ] **Step 4: Commit**

```bash
git add static/js/app.js static/css/style.css
git commit -m "feat: 3er lugar visible en el bracket público"
```

---

### Task 5: Admin — campo "Canción / tema" en el form de equipos

**Files:**
- Modify: `static/js/admin.js:1748-1829`

**Interfaces:**
- Consumes: `anthem` en teams API (Task 1), upload existente `/api/admin/upload`.

- [ ] **Step 1: Campo en el form**

En `teamFormHTML(team)` agregar tras el field-group del logo:

```js
    <div class="field-group">
      <label>Canción / tema (suena en la arena)</label>
      <div class="team-logo-actions">
        <input type="text" id="f-team-anthem" value="${escHtml(team.anthem || '')}" placeholder="URL del MP3 o sube un archivo">
        <button type="button" class="btn-ghost btn--sm" id="f-team-anthem-btn"><i data-lucide="music"></i> Subir canción</button>
        <input type="file" id="f-team-anthem-file" accept=".mp3,.m4a,.ogg,.wav" style="display:none">
        <span id="f-team-anthem-status" style="font-size:.72rem;color:var(--text-dim)"></span>
      </div>
    </div>
```

- [ ] **Step 2: Upload binding**

Después de `bindTeamLogoUpload` agregar:

```js
function bindTeamAnthemUpload() {
  const input  = document.getElementById('f-team-anthem');
  const btn    = document.getElementById('f-team-anthem-btn');
  const file   = document.getElementById('f-team-anthem-file');
  const status = document.getElementById('f-team-anthem-status');
  if (!input) return;
  btn?.addEventListener('click', () => file.click());
  file?.addEventListener('change', async () => {
    if (!file.files.length) return;
    status.textContent = 'Subiendo...';
    try {
      const fd = new FormData();
      fd.append('file', file.files[0]);
      const res  = await fetch('/api/admin/upload', { method: 'POST', body: fd });
      const data = await res.json();
      if (!res.ok) throw new Error(data.error);
      input.value = data.url;
      status.textContent = 'Listo';
    } catch (e) {
      status.textContent = 'Error: ' + e.message;
    }
  });
}
```

Llamar `bindTeamAnthemUpload()` junto a cada `bindTeamLogoUpload()` (en `addTeamBtn` y `editTeam`), y agregar `anthem` a ambos payloads:

```js
    const anthem = document.getElementById('f-team-anthem').value.trim();
    await api('POST', '/api/admin/teams', { name, logo, anthem });   // y el PUT equivalente
```

- [ ] **Step 3: Verificar en navegador**

En `/admin` → Equipos → editar un equipo: subir un MP3 corto, guardar, reabrir el form y confirmar que la URL persiste; `curl -s localhost:3000/api/teams` muestra el `anthem`.

- [ ] **Step 4: Commit**

```bash
git add static/js/admin.js
git commit -m "feat: canción por equipo en el panel admin"
```

---

### Task 6: Arena — estado base, selección de partido y sidebar de resultados

**Files:**
- Modify: `static/js/arena.js` (reemplaza el esqueleto), `static/css/arena.css`, `templates/arena.html`

**Interfaces:**
- Produces (para Tasks 7-8): estado global `S` (`phase, bracket, teams, sel, duration, hearts, remain, running, sudden, winner, muted`), helpers `team(id)`, `matchOf(sel)`, `roundLabel(sel)`, `isFinal(sel)`, `setPhase(p)`, `esc()`, `$()`, `loadBracket()`, backup `saveBackup()/loadBackup()/clearBackup()` con clave `arena-fight-backup`, y `FX` (motor de audio, definido aquí para que todas las fases lo usen).

- [ ] **Step 1: Template con contenedores por fase**

`templates/arena.html` — reemplazar el `<main>`:

```html
    <main id="arena" class="arena">
      <header class="ar-top">
        <a href="/" class="ar-brand"><i data-lucide="zap"></i> EIRI ARENA</a>
        <div class="ar-top-actions">
          <button id="ar-mute" class="ar-icon-btn" title="Silenciar (M)"><i data-lucide="volume-2"></i></button>
          <button id="ar-fs" class="ar-icon-btn" title="Pantalla completa (F)"><i data-lucide="maximize"></i></button>
        </div>
      </header>
      <section id="ph-select" class="ar-phase"></section>
      <section id="ph-intro" class="ar-phase" hidden></section>
      <section id="ph-fight" class="ar-phase" hidden></section>
      <section id="ph-victory" class="ar-phase" hidden></section>
    </main>
```

- [ ] **Step 2: Núcleo de arena.js**

Reemplazar `static/js/arena.js` completo:

```js
/* EIRI Arena — pantalla de competencia en vivo */
'use strict';

// ─── Estado global ───────────────────────────────────
const S = {
  phase: 'select',      // select | intro | fight | victory
  bracket: null,        // payload de /api/bracket
  teams: {},            // id -> team
  sel: null,            // { kind: 'round'|'third', r, i }
  duration: 180,        // segundos configurados para el combate
  hearts: { a: 3, b: 3 },
  remain: 0,            // segundos restantes (en muerte súbita: transcurridos)
  running: false,
  sudden: false,
  winner: null,         // 'a' | 'b'
  muted: false,
};

const $ = (q) => document.querySelector(q);
const esc = (s) => String(s ?? '').replace(/[&<>"']/g, c => ({ '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;' }[c]));

const team = (id) => (id != null ? S.teams[id] : null);
const matchOf = (sel) => sel.kind === 'third' ? S.bracket.third : S.bracket.rounds[sel.r][sel.i];
const isFinal = (sel) => sel.kind === 'round' && sel.r === S.bracket.rounds.length - 1;

function roundLabel(sel) {
  if (sel.kind === 'third') return '3ER LUGAR';
  const left = S.bracket.rounds.length - sel.r;
  return left === 1 ? 'GRAN FINAL' : left === 2 ? 'SEMIFINAL'
       : left === 3 ? 'CUARTOS DE FINAL' : left === 4 ? 'OCTAVOS DE FINAL' : 'RONDA';
}

// ─── Audio: efectos built-in (Web Audio) + tema por equipo ───
const FX = {
  ctx: null,
  ensure() {
    if (!this.ctx) this.ctx = new (window.AudioContext || window.webkitAudioContext)();
    if (this.ctx.state === 'suspended') this.ctx.resume();
    return this.ctx;
  },
  tone(freq, dur = 0.15, type = 'square', gain = 0.12, when = 0) {
    if (S.muted) return;
    try {
      const ctx = this.ensure();
      const o = ctx.createOscillator(), g = ctx.createGain();
      o.type = type; o.frequency.value = freq;
      g.gain.setValueAtTime(gain, ctx.currentTime + when);
      g.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + when + dur);
      o.connect(g); g.connect(ctx.destination);
      o.start(ctx.currentTime + when); o.stop(ctx.currentTime + when + dur + 0.05);
    } catch {}
  },
  beep()    { this.tone(880, 0.12); },
  go()      { this.tone(440, 0.55, 'sawtooth', 0.22); this.tone(880, 0.55, 'square', 0.1); },
  hit()     { this.tone(120, 0.35, 'sawtooth', 0.3); this.tone(75, 0.5, 'triangle', 0.25, 0.04); },
  tick()    { this.tone(1250, 0.05, 'square', 0.06); },
  alarm()   { for (let k = 0; k < 6; k++) this.tone(k % 2 ? 920 : 660, 0.16, 'square', 0.2, k * 0.18); },
  fanfare() { [523, 659, 784, 1047, 1319].forEach((f, k) => this.tone(f, 0.42, 'triangle', 0.2, k * 0.16)); },
};

const anthem = new Audio();
let fadeTimer = null;
function playAnthem(url, { fade = false } = {}) {
  stopAnthem();
  if (!url || S.muted) return;
  anthem.src = url;
  anthem.volume = fade ? 0.02 : 1;
  anthem.play().catch(() => {});   // MP3 roto o autoplay bloqueado: silencio, sin error
  if (fade) {
    fadeTimer = setInterval(() => {
      anthem.volume = Math.min(1, anthem.volume + 0.05);
      if (anthem.volume >= 1) clearInterval(fadeTimer);
    }, 120);
  }
}
function stopAnthem() { clearInterval(fadeTimer); anthem.pause(); anthem.currentTime = 0; }

// ─── Respaldo del combate en curso (a prueba de recargas) ───
const BK_KEY = 'arena-fight-backup';
function saveBackup() {
  if (S.phase !== 'fight') return;
  localStorage.setItem(BK_KEY, JSON.stringify({
    sel: S.sel, duration: S.duration, hearts: S.hearts,
    remain: S.remain, sudden: S.sudden, ts: Date.now(),
  }));
}
function loadBackup() {
  try { return JSON.parse(localStorage.getItem(BK_KEY)); } catch { return null; }
}
function clearBackup() { localStorage.removeItem(BK_KEY); }

// ─── Fases ───────────────────────────────────────────
function setPhase(p) {
  S.phase = p;
  ['select', 'intro', 'fight', 'victory'].forEach(x => { $(`#ph-${x}`).hidden = x !== p; });
  document.body.classList.toggle('is-sudden', p === 'fight' && S.sudden);
  document.body.classList.toggle('is-final', S.sel ? isFinal(S.sel) : false);
  if (p === 'select') renderSelect();
}

async function loadBracket() {
  const data = await fetch('/api/bracket').then(r => r.json());
  S.bracket = data;
  S.teams = {};
  (data.teams || []).forEach(t => { S.teams[t.id] = t; });
}

// ─── Fase ①: selección de partido ────────────────────
const playable = (m) => m && m.a != null && m.b != null && !m.winner;

function selCard(m, sel) {
  const rowHTML = (id, score, won) => {
    const t = team(id);
    return `<div class="ar-sel-row ${won ? 'is-won' : ''}">
      ${t?.logo ? `<img src="${esc(t.logo)}" class="ar-sel-logo" alt="">`
                : `<span class="ar-sel-logo ar-sel-logo--ph">${t ? esc(t.name[0]) : '·'}</span>`}
      <span class="ar-sel-name">${t ? esc(t.name) : 'Por definir'}</span>
      <span class="ar-sel-score">${esc(score)}</span>
    </div>`;
  };
  const canPlay = playable(m);
  const attr = canPlay ? `data-sel='${JSON.stringify(sel)}'` : '';
  return `<div class="ar-sel-card ${canPlay ? 'is-playable' : ''} ${m.winner ? 'is-done' : ''}" ${attr}>
    ${rowHTML(m.a, m.scoreA, m.winner === 'a')}
    ${rowHTML(m.b, m.scoreB, m.winner === 'b')}
  </div>`;
}

function resultsSidebar() {
  const done = [];
  S.bracket.rounds.forEach((round, r) => round.forEach((m, i) => {
    if (m.winner) done.push({ m, label: roundLabel({ kind: 'round', r, i }) });
  }));
  if (S.bracket.third?.winner) done.push({ m: S.bracket.third, label: '3ER LUGAR' });
  if (!done.length) return '<p class="ar-side-empty">Aún no hay combates jugados.</p>';
  return done.map(({ m, label }) => {
    const w = team(m.winner === 'a' ? m.a : m.b), l = team(m.winner === 'a' ? m.b : m.a);
    const sw = m.winner === 'a' ? m.scoreA : m.scoreB, sl = m.winner === 'a' ? m.scoreB : m.scoreA;
    return `<div class="ar-side-item"><span class="ar-side-label">${label}</span>
      <strong>${esc(w?.name || '?')}</strong> eliminó a ${esc(l?.name || '?')} ${esc(sw)}–${esc(sl)}</div>`;
  }).reverse().join('');
}

function renderSelect() {
  const b = S.bracket;
  const el = $('#ph-select');
  const backup = loadBackup();
  const resumable = backup && playable(matchOf(backup.sel) || {});

  const roundsHTML = b.rounds.map((round, r) => `
    <div class="ar-sel-round">
      <div class="ar-sel-round-title">${roundLabel({ kind: 'round', r, i: 0 })}</div>
      ${round.map((m, i) => selCard(m, { kind: 'round', r, i })).join('')}
    </div>`).join('');

  const thirdHTML = (b.third && (b.third.a != null || b.third.b != null)) ? `
    <div class="ar-sel-round ar-sel-round--third">
      <div class="ar-sel-round-title">3er lugar</div>
      ${selCard(b.third, { kind: 'third' })}
    </div>` : '';

  el.innerHTML = `
    ${resumable ? `<div class="ar-resume">
        Hay un combate en curso sin terminar.
        <button class="ar-btn ar-btn--gold" id="ar-resume-btn">Reanudar combate</button>
        <button class="ar-btn ar-btn--ghost" id="ar-resume-discard">Descartar</button>
      </div>` : ''}
    <div class="ar-sel-layout">
      <div class="ar-sel-main">
        <h1 class="ar-title">${esc(b.title)}</h1>
        <p class="ar-sub">Elige el próximo combate — los partidos listos brillan en dorado</p>
        <div class="ar-sel-rounds">${roundsHTML}${thirdHTML}</div>
        <div class="ar-timer-cfg">
          <span>Duración del combate</span>
          <button class="ar-btn ar-btn--ghost" id="ar-dur-minus">−30s</button>
          <strong id="ar-dur">${fmtClock(S.duration)}</strong>
          <button class="ar-btn ar-btn--ghost" id="ar-dur-plus">+30s</button>
        </div>
      </div>
      <aside class="ar-side">
        <h2 class="ar-side-title">Resultados de la jornada</h2>
        ${resultsSidebar()}
      </aside>
    </div>`;

  el.querySelectorAll('.ar-sel-card.is-playable').forEach(card => {
    card.addEventListener('click', () => {
      S.sel = JSON.parse(card.dataset.sel);
      startIntro();
    });
  });
  $('#ar-dur-minus')?.addEventListener('click', () => { S.duration = Math.max(30, S.duration - 30); $('#ar-dur').textContent = fmtClock(S.duration); });
  $('#ar-dur-plus')?.addEventListener('click', () => { S.duration = Math.min(1200, S.duration + 30); $('#ar-dur').textContent = fmtClock(S.duration); });
  $('#ar-resume-btn')?.addEventListener('click', () => resumeFight(backup));
  $('#ar-resume-discard')?.addEventListener('click', () => { clearBackup(); renderSelect(); });
  lucide.createIcons({ nodes: [el] });
}

function fmtClock(s) {
  return `${Math.floor(s / 60)}:${String(s % 60).padStart(2, '0')}`;
}

// Placeholders que las Tasks 7-8 reemplazan:
function startIntro() { setPhase('intro'); $('#ph-intro').innerHTML = '<p>intro pendiente</p>'; }
function resumeFight(backup) { console.log('resume pendiente', backup); }

// ─── Controles globales ──────────────────────────────
$('#ar-mute').addEventListener('click', () => {
  S.muted = !S.muted;
  if (S.muted) stopAnthem();
  $('#ar-mute').innerHTML = `<i data-lucide="${S.muted ? 'volume-x' : 'volume-2'}"></i>`;
  lucide.createIcons({ nodes: [$('#ar-mute')] });
});
$('#ar-fs').addEventListener('click', toggleFullscreen);
function toggleFullscreen() {
  if (document.fullscreenElement) document.exitFullscreen();
  else document.documentElement.requestFullscreen().catch(() => {});
}
document.addEventListener('keydown', (e) => {
  if (e.key === 'f' || e.key === 'F') toggleFullscreen();
  if (e.key === 'm' || e.key === 'M') $('#ar-mute').click();
});

// ─── Init ────────────────────────────────────────────
(async function init() {
  try {
    await loadBracket();
    setPhase('select');
  } catch {
    $('#ph-select').innerHTML = `<div class="ar-error">
      No se pudo cargar el bracket. <button class="ar-btn" onclick="location.reload()">Reintentar</button>
    </div>`;
  }
  lucide.createIcons();
})();
```

- [ ] **Step 3: CSS de la fase de selección**

Reemplazar `static/css/arena.css`:

```css
/* EIRI Arena — pantalla de competencia (proyector: todo grande y oscuro) */
.arena-body { overflow-x: hidden; background: #060b16; }
.arena { min-height: 100vh; display: flex; flex-direction: column; }
.ar-phase { flex: 1; padding: 1rem 2rem 2rem; }

.ar-top {
  display: flex; justify-content: space-between; align-items: center;
  padding: 0.8rem 2rem; border-bottom: 1px solid var(--border);
}
.ar-brand {
  font-family: var(--f-display); font-weight: 700; letter-spacing: 0.18em;
  color: var(--gold-300); text-decoration: none; display: flex; gap: 0.5rem; align-items: center;
}
.ar-brand svg { width: 18px; height: 18px; }
.ar-top-actions { display: flex; gap: 0.5rem; }
.ar-icon-btn {
  background: none; border: 1px solid var(--border-md); border-radius: 8px;
  color: var(--text); width: 38px; height: 38px; cursor: pointer;
  display: flex; align-items: center; justify-content: center;
}
.ar-icon-btn:hover { border-color: var(--gold-400); color: var(--gold-300); }
.ar-icon-btn svg { width: 17px; height: 17px; }

.ar-btn {
  font-family: var(--f-display); font-weight: 600; border-radius: 9px; cursor: pointer;
  padding: 0.55rem 1.2rem; border: 1px solid var(--border-md);
  background: var(--bg-2); color: var(--text); transition: all 0.2s var(--ease-out);
}
.ar-btn--gold { background: linear-gradient(135deg, var(--gold-400), #b8860b); color: #100c02; border: none; }
.ar-btn--gold:hover { filter: brightness(1.15); }
.ar-btn--ghost { background: none; }
.ar-btn--ghost:hover { border-color: var(--gold-400); }

.ar-title { font-family: var(--f-display); font-size: 1.6rem; color: #fff; }
.ar-sub { color: var(--text-dim); font-size: 0.85rem; margin-bottom: 1.2rem; }
.ar-error { padding: 3rem; text-align: center; color: var(--text-dim); }

/* Selección */
.ar-sel-layout { display: grid; grid-template-columns: 1fr 280px; gap: 1.6rem; }
.ar-sel-rounds { display: flex; gap: 1.2rem; align-items: flex-start; overflow-x: auto; padding-bottom: 0.6rem; }
.ar-sel-round { display: flex; flex-direction: column; gap: 0.7rem; min-width: 200px; }
.ar-sel-round-title {
  font-family: var(--f-mono); font-size: 0.6rem; letter-spacing: 0.22em;
  text-transform: uppercase; color: var(--text-dim); text-align: center;
}
.ar-sel-round--third .ar-sel-round-title { color: #cd7f32; }
.ar-sel-card {
  border: 1px solid var(--border-md); border-radius: 10px; overflow: hidden;
  background: rgba(8,16,30,0.94); opacity: 0.75;
}
.ar-sel-card.is-playable {
  opacity: 1; cursor: pointer;
  border-color: rgba(227,189,63,0.55);
  box-shadow: 0 0 18px -6px var(--gold-glow);
  animation: arGlow 2.2s ease-in-out infinite;
}
.ar-sel-card.is-playable:hover { transform: translateY(-2px); border-color: var(--gold-300); }
@keyframes arGlow { 0%,100% { box-shadow: 0 0 14px -8px var(--gold-glow); } 50% { box-shadow: 0 0 26px -4px var(--gold-glow); } }
.ar-sel-row { display: flex; align-items: center; gap: 0.55rem; padding: 0.5rem 0.7rem; }
.ar-sel-row + .ar-sel-row { border-top: 1px solid var(--border); }
.ar-sel-row.is-won { background: linear-gradient(90deg, rgba(201,162,39,0.2), transparent); }
.ar-sel-logo { width: 30px; height: 30px; border-radius: 6px; object-fit: cover; background: var(--bg-2); flex-shrink: 0; }
.ar-sel-logo--ph {
  display: flex; align-items: center; justify-content: center;
  font-family: var(--f-mono); font-size: 0.7rem; color: var(--gold-300);
  border: 1px solid var(--border); text-transform: uppercase;
}
.ar-sel-name { flex: 1; font-size: 0.82rem; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.ar-sel-score { font-family: var(--f-mono); font-size: 0.8rem; font-weight: 700; color: var(--text-dim); }

.ar-timer-cfg { display: flex; align-items: center; gap: 0.8rem; margin-top: 1.4rem; color: var(--text-dim); font-size: 0.85rem; }
.ar-timer-cfg strong { font-family: var(--f-mono); font-size: 1.3rem; color: #fff; min-width: 4ch; text-align: center; }

.ar-side { border-left: 1px solid var(--border); padding-left: 1.4rem; }
.ar-side-title { font-family: var(--f-mono); font-size: 0.62rem; letter-spacing: 0.22em; text-transform: uppercase; color: var(--text-dim); margin-bottom: 0.9rem; }
.ar-side-item { font-size: 0.8rem; color: var(--text-dim); padding: 0.5rem 0; border-bottom: 1px solid var(--border); }
.ar-side-item strong { color: var(--gold-300); }
.ar-side-label { display: block; font-family: var(--f-mono); font-size: 0.55rem; letter-spacing: 0.18em; color: var(--text-dim); opacity: 0.7; }
.ar-side-empty { color: var(--text-dim); font-size: 0.8rem; opacity: 0.7; }

.ar-resume {
  display: flex; align-items: center; gap: 0.9rem; margin-bottom: 1.2rem;
  padding: 0.8rem 1.1rem; border: 1px solid rgba(227,189,63,0.5); border-radius: 10px;
  background: rgba(227,189,63,0.08); font-size: 0.88rem;
}
```

- [ ] **Step 4: Verificar en navegador**

`localhost:3000/arena` (con sesión): se ve el bracket por rondas, los partidos con ambos equipos y sin ganador brillan dorado y son clickeables (clic → texto "intro pendiente"), el timer configurable cambia con ±30s, la sidebar lista resultados jugados. Mute y fullscreen (M/F) funcionan.

- [ ] **Step 5: Commit**

```bash
git add templates/arena.html static/js/arena.js static/css/arena.css
git commit -m "feat: arena — selección de partido, timer configurable y resultados"
```

---

### Task 7: Arena — presentación VS

**Files:**
- Modify: `static/js/arena.js` (reemplaza el placeholder `startIntro`), `static/css/arena.css`

**Interfaces:**
- Consumes: `S`, `team`, `matchOf`, `roundLabel`, `playAnthem`, `stopAnthem`, `setPhase`, `esc`.
- Produces: `startIntro()` real; botón "¡A la arena!" llama `startFight()` (placeholder hasta Task 8).

- [ ] **Step 1: Implementar startIntro**

Reemplazar el placeholder `startIntro` en arena.js:

```js
// ─── Fase ②: presentación VS ─────────────────────────
function startIntro() {
  const m = matchOf(S.sel);
  const A = team(m.a), B = team(m.b);
  setPhase('intro');
  const side = (t, dir) => `
    <div class="ar-vs-side ar-vs-side--${dir}">
      ${t.logo ? `<img src="${esc(t.logo)}" class="ar-vs-logo" alt="">`
               : `<span class="ar-vs-logo ar-vs-logo--ph">${esc(t.name[0])}</span>`}
      <h2 class="ar-vs-name">${esc(t.name)}</h2>
      ${t.anthem ? `<button class="ar-btn ar-btn--ghost ar-anthem-btn" data-url="${esc(t.anthem)}">
        <i data-lucide="music"></i> Tema</button>` : ''}
    </div>`;
  $('#ph-intro').innerHTML = `
    <div class="ar-vs">
      <div class="ar-vs-round">${roundLabel(S.sel)}</div>
      <div class="ar-vs-stage">
        ${side(A, 'left')}
        <div class="ar-vs-mark">VS</div>
        ${side(B, 'right')}
      </div>
      <div class="ar-vs-actions">
        <button class="ar-btn ar-btn--ghost" id="ar-vs-back"><i data-lucide="arrow-left"></i> Volver</button>
        <button class="ar-btn ar-btn--ghost" id="ar-vs-stop"><i data-lucide="square"></i> Detener música</button>
        <button class="ar-btn ar-btn--gold ar-btn--big" id="ar-vs-go">¡A LA ARENA!</button>
      </div>
    </div>`;
  $('#ph-intro').querySelectorAll('.ar-anthem-btn').forEach(b =>
    b.addEventListener('click', () => playAnthem(b.dataset.url)));
  $('#ar-vs-stop').addEventListener('click', stopAnthem);
  $('#ar-vs-back').addEventListener('click', () => { stopAnthem(); setPhase('select'); });
  $('#ar-vs-go').addEventListener('click', () => { stopAnthem(); startFight(); });
  lucide.createIcons({ nodes: [$('#ph-intro')] });
}

function startFight() { console.log('fight pendiente'); }   // Task 8 lo reemplaza
```

- [ ] **Step 2: CSS de la presentación**

Agregar a arena.css:

```css
/* Presentación VS */
.ar-vs { display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: calc(100vh - 130px); gap: 2rem; }
.ar-vs-round {
  font-family: var(--f-display); font-size: clamp(1.2rem, 3vw, 2.2rem); font-weight: 700;
  letter-spacing: 0.35em; color: var(--gold-300); text-transform: uppercase;
  animation: arDrop 0.6s var(--ease-out) both;
}
.ar-vs-stage { display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; gap: 3rem; width: 100%; max-width: 1100px; }
.ar-vs-side { display: flex; flex-direction: column; align-items: center; gap: 1rem; }
.ar-vs-side--left  { animation: arFromLeft 0.7s var(--ease-out) both 0.15s; }
.ar-vs-side--right { animation: arFromRight 0.7s var(--ease-out) both 0.15s; }
.ar-vs-logo { width: clamp(120px, 18vw, 230px); height: clamp(120px, 18vw, 230px); border-radius: 22px; object-fit: cover; background: var(--bg-2); box-shadow: 0 20px 60px -20px rgba(0,0,0,0.9); }
.ar-vs-logo--ph { display: flex; align-items: center; justify-content: center; font-family: var(--f-display); font-size: 4rem; color: var(--gold-300); border: 2px solid var(--border-md); }
.ar-vs-name { font-family: var(--f-display); font-size: clamp(1.2rem, 2.6vw, 2rem); color: #fff; text-align: center; }
.ar-vs-mark {
  font-family: var(--f-display); font-size: clamp(2.5rem, 6vw, 5rem); font-weight: 900;
  color: var(--gold-400); text-shadow: 0 0 40px var(--gold-glow);
  animation: arPop 0.5s var(--ease-out) both 0.5s;
}
.ar-vs-actions { display: flex; gap: 1rem; align-items: center; }
.ar-btn--big { font-size: 1.15rem; padding: 0.9rem 2.4rem; letter-spacing: 0.08em; }
@keyframes arFromLeft  { from { opacity: 0; transform: translateX(-80px); } to { opacity: 1; transform: none; } }
@keyframes arFromRight { from { opacity: 0; transform: translateX(80px); }  to { opacity: 1; transform: none; } }
@keyframes arPop  { from { opacity: 0; transform: scale(0.3); } to { opacity: 1; transform: scale(1); } }
@keyframes arDrop { from { opacity: 0; transform: translateY(-24px); } to { opacity: 1; transform: none; } }
```

- [ ] **Step 3: Verificar en navegador**

Clic en un partido jugable → logos entran animados desde los lados, "VS" con pop, nombre de la ronda arriba. Botón "Tema" reproduce el MP3 del equipo (si se configuró en Task 5), "Detener música" lo corta, "Volver" regresa a selección, "¡A LA ARENA!" imprime `fight pendiente` en consola.

- [ ] **Step 4: Commit**

```bash
git add static/js/arena.js static/css/arena.css
git commit -m "feat: arena — presentación VS con temas por equipo"
```

---

### Task 8: Arena — combate (timer, corazones, teclado, muerte súbita, respaldo)

**Files:**
- Modify: `static/js/arena.js` (reemplaza `startFight` y `resumeFight`), `static/css/arena.css`

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: `startFight()`, `resumeFight(backup)`, y `endFight(winner)` que llama `showVictory()` (placeholder hasta Task 9).

- [ ] **Step 1: Implementar el combate**

Reemplazar los placeholders `startFight`/`resumeFight` en arena.js:

```js
// ─── Fase ③: combate ─────────────────────────────────
let tickHandle = null, endAt = 0;

function startFight() {
  S.hearts = { a: 3, b: 3 };
  S.remain = S.duration;
  S.sudden = false;
  S.winner = null;
  renderFight();
  runCountdown(() => { FX.go(); resumeTimer(); saveBackup(); });
}

function resumeFight(backup) {
  S.sel = backup.sel;
  S.duration = backup.duration;
  S.hearts = backup.hearts;
  S.remain = backup.remain;
  S.sudden = backup.sudden;
  S.winner = null;
  renderFight();
  setPaused(true);   // se reanuda con Espacio o el botón
}

function renderFight() {
  const m = matchOf(S.sel);
  const A = team(m.a), B = team(m.b);
  setPhase('fight');
  const side = (t, key) => `
    <div class="ar-f-side" id="ar-f-${key}">
      ${t.logo ? `<img src="${esc(t.logo)}" class="ar-f-logo" alt="">`
               : `<span class="ar-f-logo ar-f-logo--ph">${esc(t.name[0])}</span>`}
      <h2 class="ar-f-name">${esc(t.name)}</h2>
      <div class="ar-f-hearts" data-side="${key}">${heartsHTML(key)}</div>
      <div class="ar-f-keys">${key === 'a' ? 'Q quita · A devuelve' : 'P quita · L devuelve'}</div>
    </div>`;
  $('#ph-fight').innerHTML = `
    <div class="ar-f">
      <div class="ar-f-round">${roundLabel(S.sel)}</div>
      <div class="ar-f-stage">
        ${side(A, 'a')}
        <div class="ar-f-center">
          <div class="ar-f-clock" id="ar-clock">${fmtClock(S.remain)}</div>
          <div class="ar-f-sudden-label" id="ar-sudden-label" hidden>MUERTE SÚBITA</div>
          <button class="ar-btn ar-btn--ghost" id="ar-pause">Pausa (Espacio)</button>
          <button class="ar-btn ar-btn--ghost" id="ar-abort">Cancelar combate</button>
        </div>
        ${side(B, 'b')}
      </div>
      <div class="ar-f-countdown" id="ar-countdown" hidden></div>
    </div>`;
  $('#ph-fight').querySelectorAll('.ar-f-hearts').forEach(el => {
    el.addEventListener('click', (e) => {
      const h = e.target.closest('.ar-heart');
      if (!h) return;
      const side = el.dataset.side;
      if (h.classList.contains('is-lost')) giveHeart(side); else loseHeart(side);
    });
  });
  if (isFinal(S.sel)) spawnSparks();
  $('#ar-pause').addEventListener('click', () => setPaused(S.running));
  $('#ar-abort').addEventListener('click', () => {
    if (!window.confirm('¿Cancelar este combate? No se guardará nada.')) return;
    pauseTimer(); clearBackup(); stopAnthem(); setPhase('select');
  });
  refreshSuddenUI();
}

function heartsHTML(side) {
  return [0, 1, 2].map(k =>
    `<span class="ar-heart ${k >= S.hearts[side] ? 'is-lost' : ''}">${k >= S.hearts[side] ? '🖤' : '❤️'}</span>`
  ).join('');
}
function renderHearts(side, shake = false) {
  const el = $(`#ph-fight .ar-f-hearts[data-side="${side}"]`);
  el.innerHTML = heartsHTML(side);
  if (shake) {
    const box = $(`#ar-f-${side}`);
    box.classList.remove('is-hit'); void box.offsetWidth;   // reinicia la animación
    box.classList.add('is-hit');
  }
}

function runCountdown(done) {
  const el = $('#ar-countdown');
  el.hidden = false;
  const seq = ['3', '2', '1', '¡PELEA!'];
  let k = 0;
  const step = () => {
    if (k < 3) FX.beep();
    el.textContent = seq[k];
    el.classList.remove('pop'); void el.offsetWidth; el.classList.add('pop');
    k++;
    if (k < seq.length) setTimeout(step, 900);
    else setTimeout(() => { el.hidden = true; done(); }, 700);
  };
  step();
}

// Timer sin deriva: recalcula contra Date.now() en cada tick.
function resumeTimer() {
  S.running = true;
  endAt = S.sudden ? Date.now() - S.remain * 1000 : Date.now() + S.remain * 1000;
  clearInterval(tickHandle);
  tickHandle = setInterval(onTick, 200);
  $('#ar-pause').textContent = 'Pausa (Espacio)';
}
function pauseTimer() {
  S.running = false;
  clearInterval(tickHandle);
}
function setPaused(paused) {
  if (paused) { pauseTimer(); $('#ar-pause').textContent = 'Reanudar (Espacio)'; }
  else resumeTimer();
}

function onTick() {
  if (S.sudden) {
    const t = Math.floor((Date.now() - endAt) / 1000);
    if (t !== S.remain) { S.remain = t; updateClock(); saveBackup(); }
    return;
  }
  const left = Math.ceil((endAt - Date.now()) / 1000);
  if (left !== S.remain) {
    S.remain = Math.max(0, left);
    if (S.remain <= 10 && S.remain > 0) FX.tick();
    updateClock();
    saveBackup();
    if (S.remain === 0) onTimeUp();
  }
}
function updateClock() {
  const el = $('#ar-clock');
  el.textContent = fmtClock(S.remain);
  el.classList.toggle('is-low', !S.sudden && S.remain <= 10);
}

function loseHeart(side) {
  if (S.phase !== 'fight' || S.winner || S.hearts[side] === 0) return;
  S.hearts[side]--;
  FX.hit();
  renderHearts(side, true);
  saveBackup();
  const other = side === 'a' ? 'b' : 'a';
  if (S.hearts[side] === 0 || S.sudden) endFight(other);
}
function giveHeart(side) {
  if (S.phase !== 'fight' || S.winner || S.hearts[side] === 3) return;
  S.hearts[side]++;
  renderHearts(side);
  saveBackup();
}

function onTimeUp() {
  pauseTimer();
  if (S.hearts.a !== S.hearts.b) return endFight(S.hearts.a > S.hearts.b ? 'a' : 'b');
  // Empate → muerte súbita: el próximo corazón decide, el reloj sube desde 0.
  S.sudden = true;
  S.remain = 0;
  FX.alarm();
  refreshSuddenUI();
  resumeTimer();
  saveBackup();
}
function refreshSuddenUI() {
  document.body.classList.toggle('is-sudden', S.sudden);
  const lbl = $('#ar-sudden-label');
  if (lbl) lbl.hidden = !S.sudden;
}

function endFight(winner) {
  pauseTimer();
  S.winner = winner;
  document.body.classList.remove('is-sudden');
  showVictory();
}
function showVictory() { console.log('victoria pendiente', S.winner); }   // Task 9

// Partículas doradas flotando durante la GRAN FINAL
function spawnSparks() {
  const box = document.createElement('div');
  box.className = 'ar-f-sparks';
  for (let k = 0; k < 26; k++) {
    const p = document.createElement('i');
    p.style.cssText = `left:${Math.random() * 100}%;top:${Math.random() * 100}%;` +
      `animation-delay:${Math.random() * 6}s;animation-duration:${5 + Math.random() * 6}s;`;
    box.appendChild(p);
  }
  $('.ar-f').appendChild(box);
}

// Teclado del combate
document.addEventListener('keydown', (e) => {
  if (S.phase !== 'fight' || S.winner) return;
  const k = e.key.toLowerCase();
  if (k === 'q') loseHeart('a');
  if (k === 'a') giveHeart('a');
  if (k === 'p') loseHeart('b');
  if (k === 'l') giveHeart('b');
  if (e.code === 'Space') { e.preventDefault(); setPaused(S.running); }
});
```

Nota: en `renderSelect` ya existe `resumeFight(backup)` conectado al banner de reanudar.

- [ ] **Step 2: CSS del combate**

Agregar a arena.css:

```css
/* Combate */
.ar-f { display: flex; flex-direction: column; align-items: center; min-height: calc(100vh - 130px); justify-content: center; gap: 1.6rem; position: relative; }
.ar-f-round { font-family: var(--f-display); font-size: clamp(1rem, 2.4vw, 1.7rem); letter-spacing: 0.35em; color: var(--gold-300); text-transform: uppercase; }
.ar-f-stage { display: grid; grid-template-columns: 1fr auto 1fr; gap: 3rem; align-items: center; width: 100%; max-width: 1200px; }
.ar-f-side { display: flex; flex-direction: column; align-items: center; gap: 0.9rem; padding: 1.4rem; border-radius: 18px; transition: background 0.2s; }
.ar-f-side.is-hit { animation: arHit 0.45s var(--ease-out); }
@keyframes arHit {
  0% { background: rgba(239,68,68,0.35); transform: translateX(0); }
  25% { transform: translateX(-9px); } 50% { transform: translateX(8px); }
  75% { transform: translateX(-4px); } 100% { background: transparent; transform: none; }
}
.ar-f-logo { width: clamp(110px, 15vw, 190px); height: clamp(110px, 15vw, 190px); border-radius: 20px; object-fit: cover; background: var(--bg-2); }
.ar-f-logo--ph { display: flex; align-items: center; justify-content: center; font-family: var(--f-display); font-size: 3.4rem; color: var(--gold-300); border: 2px solid var(--border-md); }
.ar-f-name { font-family: var(--f-display); font-size: clamp(1.1rem, 2.2vw, 1.7rem); color: #fff; text-align: center; }
.ar-f-hearts { display: flex; gap: 0.5rem; font-size: clamp(1.8rem, 3.5vw, 2.8rem); cursor: pointer; user-select: none; }
.ar-heart { transition: transform 0.15s; }
.ar-heart:hover { transform: scale(1.2); }
.ar-heart.is-lost { opacity: 0.45; filter: grayscale(0.6); }
.ar-f-keys { font-family: var(--f-mono); font-size: 0.6rem; letter-spacing: 0.14em; color: var(--text-dim); opacity: 0.7; }
.ar-f-center { display: flex; flex-direction: column; align-items: center; gap: 0.9rem; }
.ar-f-clock {
  font-family: var(--f-mono); font-weight: 700; font-size: clamp(3.4rem, 9vw, 7rem);
  color: #fff; line-height: 1; font-variant-numeric: tabular-nums;
}
.ar-f-clock.is-low { color: #ef4444; animation: arPulse 1s infinite; }
@keyframes arPulse { 0%,100% { transform: scale(1); } 50% { transform: scale(1.06); } }
.ar-f-sudden-label {
  font-family: var(--f-display); font-weight: 900; letter-spacing: 0.3em;
  color: #ef4444; font-size: clamp(1rem, 2.5vw, 1.6rem);
  animation: arPulse 0.8s infinite;
}
.ar-f-countdown {
  position: fixed; inset: 0; display: flex; align-items: center; justify-content: center;
  background: rgba(3,7,14,0.82); z-index: 40;
  font-family: var(--f-display); font-weight: 900; font-size: clamp(6rem, 20vw, 15rem); color: var(--gold-300);
}
.ar-f-countdown.pop { animation: arPop 0.4s var(--ease-out); }

/* Modo muerte súbita: toda la pantalla vira a rojo */
body.is-sudden .arena { background: radial-gradient(ellipse at center, rgba(90,8,8,0.55), transparent 75%); }
body.is-sudden .ar-f-clock { color: #ef4444; }

/* La GRAN FINAL lleva marco dorado y partículas flotando */
body.is-final .ar-f-stage { border: 1px solid rgba(227,189,63,0.4); border-radius: 24px; box-shadow: 0 0 60px -25px var(--gold-glow), inset 0 0 60px -40px var(--gold-glow); padding: 1.5rem; }
.ar-f-sparks { position: absolute; inset: 0; pointer-events: none; overflow: hidden; }
.ar-f-sparks i {
  position: absolute; width: 4px; height: 4px; border-radius: 50%;
  background: var(--gold-300); opacity: 0;
  box-shadow: 0 0 8px 2px var(--gold-glow);
  animation-name: arSpark; animation-iteration-count: infinite; animation-timing-function: ease-in-out;
}
@keyframes arSpark {
  0%, 100% { opacity: 0; transform: translateY(0); }
  20%, 80% { opacity: 0.85; }
  50% { transform: translateY(-60px); }
}
```

- [ ] **Step 3: Verificar en navegador (checklist)**

1. "¡A LA ARENA!" → countdown 3-2-1-¡PELEA! con beeps y gong; el timer arranca.
2. Q/P quitan corazones (animación de golpe + sonido); A/L los devuelven; click en corazones también funciona.
3. Espacio pausa y reanuda; el reloj no deriva (pausar 10s y comparar).
4. Tercer corazón de un lado → pasa a `victoria pendiente` en consola con el ganador correcto.
5. Dejar correr el tiempo con corazones distintos → gana el de más corazones.
6. Dejar correr el tiempo con corazones iguales → alarma + "MUERTE SÚBITA" + fondo rojo + reloj sube; el próximo corazón termina el combate.
7. A mitad de combate, recargar la página → banner "Reanudar combate" restaura corazones y tiempo (pausado).
8. Últimos 10 segundos: reloj rojo pulsante con tick por segundo.
9. En la final (si ambos finalistas existen): marco dorado alrededor del escenario.

- [ ] **Step 4: Commit**

```bash
git add static/js/arena.js static/css/arena.css
git commit -m "feat: arena — combate con timer, corazones y muerte súbita"
```

---

### Task 9: Arena — victoria, confeti y confirmación del resultado

**Files:**
- Modify: `static/js/arena.js` (reemplaza `showVictory`), `static/css/arena.css`

**Interfaces:**
- Consumes: `PUT /api/admin/bracket/match` (Task 3), `playAnthem`, `FX.fanfare`, `clearBackup`, `loadBracket`, `setPhase`.

- [ ] **Step 1: Implementar showVictory + confirmación**

Reemplazar el placeholder `showVictory` en arena.js:

```js
// ─── Fase ④: victoria ────────────────────────────────
function showVictory() {
  const m = matchOf(S.sel);
  const W = team(S.winner === 'a' ? m.a : m.b);
  setPhase('victory');
  $('#ph-victory').innerHTML = `
    <div class="ar-v">
      <div class="ar-v-confetti" id="ar-confetti"></div>
      <div class="ar-v-crown"><i data-lucide="crown"></i></div>
      ${W.logo ? `<img src="${esc(W.logo)}" class="ar-v-logo" alt="">`
               : `<span class="ar-v-logo ar-v-logo--ph">${esc(W.name[0])}</span>`}
      <h1 class="ar-v-name">${esc(W.name)}</h1>
      <div class="ar-v-label">${S.sel.kind === 'third' ? '¡TERCER LUGAR!' : isFinal(S.sel) ? '¡CAMPEÓN DEL TORNEO!' : '¡VICTORIA!'}</div>
      <div class="ar-v-score">${S.hearts.a} — ${S.hearts.b}</div>
      <p class="ar-v-err" id="confirmErr"></p>
      <div class="ar-v-actions">
        <button class="ar-btn ar-btn--ghost" id="ar-v-discard">Descartar</button>
        <button class="ar-btn ar-btn--gold ar-btn--big" id="ar-v-confirm">Confirmar resultado</button>
      </div>
    </div>`;
  spawnConfetti();
  if (W.anthem) playAnthem(W.anthem, { fade: true }); else FX.fanfare();
  $('#ar-v-confirm').addEventListener('click', confirmResult);
  $('#ar-v-discard').addEventListener('click', () => {
    if (!window.confirm('¿Descartar el resultado? No se guardará nada.')) return;
    stopAnthem(); clearBackup(); setPhase('select');
  });
  lucide.createIcons({ nodes: [$('#ph-victory')] });
}

function spawnConfetti() {
  const box = $('#ar-confetti');
  const colors = ['#e3bd3f', '#f4d774', '#42a5f5', '#ef4444', '#4ade80', '#ffffff'];
  for (let k = 0; k < 120; k++) {
    const p = document.createElement('i');
    p.style.cssText = `left:${Math.random() * 100}%;background:${colors[k % colors.length]};` +
      `animation-delay:${Math.random() * 2.5}s;animation-duration:${2.4 + Math.random() * 2.4}s;` +
      `width:${5 + Math.random() * 7}px;height:${8 + Math.random() * 8}px;`;
    box.appendChild(p);
  }
}

let saving = false;
async function confirmResult() {
  if (saving) return;
  saving = true;
  $('#ar-v-confirm').textContent = 'Guardando...';
  const body = {
    scoreA: String(S.hearts.a),
    scoreB: String(S.hearts.b),
    winner: S.winner,
    ...(S.sel.kind === 'third' ? { third: true } : { round: S.sel.r, index: S.sel.i }),
  };
  try {
    const res = await fetch('/api/admin/bracket/match', {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(body),
    });
    if (!res.ok) throw new Error((await res.json().catch(() => ({}))).error || `HTTP ${res.status}`);
    clearBackup();
    stopAnthem();
    await loadBracket();
    setPhase('select');
  } catch (e) {
    $('#confirmErr').textContent = `No se pudo guardar: ${e.message} — puedes reintentar.`;
    $('#ar-v-confirm').textContent = 'Confirmar resultado';
  } finally {
    saving = false;
  }
}
```

- [ ] **Step 2: CSS de la victoria**

Agregar a arena.css:

```css
/* Victoria */
.ar-v { display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: calc(100vh - 130px); gap: 1rem; position: relative; overflow: hidden; }
.ar-v-crown svg { width: 54px; height: 54px; color: var(--gold-400); filter: drop-shadow(0 0 14px var(--gold-glow)); animation: arDrop 0.5s var(--ease-out) both; }
.ar-v-logo { width: clamp(150px, 22vw, 260px); height: clamp(150px, 22vw, 260px); border-radius: 26px; object-fit: cover; background: var(--bg-2); border: 2px solid rgba(227,189,63,0.5); box-shadow: 0 0 80px -20px var(--gold-glow); animation: arPop 0.55s var(--ease-out) both 0.1s; }
.ar-v-logo--ph { display: flex; align-items: center; justify-content: center; font-family: var(--f-display); font-size: 5rem; color: var(--gold-300); }
.ar-v-name { font-family: var(--f-display); font-size: clamp(1.8rem, 4.5vw, 3.4rem); color: #fff; animation: arDrop 0.5s var(--ease-out) both 0.25s; }
.ar-v-label { font-family: var(--f-display); letter-spacing: 0.35em; color: var(--gold-300); font-size: clamp(0.9rem, 2vw, 1.4rem); animation: arDrop 0.5s var(--ease-out) both 0.35s; }
.ar-v-score { font-family: var(--f-mono); font-size: clamp(1.6rem, 3.5vw, 2.6rem); font-weight: 700; color: var(--text-dim); }
.ar-v-actions { display: flex; gap: 1rem; margin-top: 0.6rem; }
.ar-v-err { color: #ef4444; font-size: 0.85rem; min-height: 1.2em; }
.ar-v-confetti { position: absolute; inset: 0; pointer-events: none; }
.ar-v-confetti i {
  position: absolute; top: -20px; border-radius: 2px; opacity: 0.9;
  animation-name: arConfetti; animation-timing-function: linear; animation-iteration-count: infinite;
}
@keyframes arConfetti {
  0% { transform: translateY(-20px) rotate(0deg); }
  100% { transform: translateY(105vh) rotate(720deg); }
}
```

- [ ] **Step 3: Verificar flujo completo (checklist de evento)**

1. Combate completo → victoria: confeti cayendo, corona, logo grande, marcador en corazones, canción del ganador (o fanfarria si no tiene).
2. "Confirmar resultado" → vuelve a selección; el partido aparece jugado con su marcador y el ganador ya está en la ronda siguiente; la sidebar registra el combate; el bracket público (`localhost:3000/#bracket`) refleja lo mismo.
3. Jugar ambas semis → el partido de 3er lugar aparece jugable en la arena con los dos perdedores.
4. Jugar el 3er lugar y confirmar → aparece "3er lugar" con 🥉 en la home.
5. Jugar la final → etiqueta "¡CAMPEÓN DEL TORNEO!" y el campeón coronado en la home.
6. "Descartar" no escribe nada (verificar con `curl -s localhost:3000/api/bracket`).
7. Con el server detenido, "Confirmar" muestra el error con opción de reintentar sin perder la pantalla.

- [ ] **Step 4: Commit**

```bash
git add static/js/arena.js static/css/arena.css
git commit -m "feat: arena — victoria con confeti y confirmación al bracket"
```

---

### Task 10: Verificación final y deploy

**Files:** ninguno nuevo.

- [ ] **Step 1: Flujo end-to-end limpio**

Resetear el bracket desde el admin (tamaño 8, colocar los 8 equipos reales), y correr el checklist del Task 9 Step 3 completo de principio a fin en una pestaña limpia (torneo entero: 4 cuartos → 2 semis → 3er lugar → final).

- [ ] **Step 2: Revisión de código**

Usar el skill superpowers:requesting-code-review sobre el diff completo de la rama antes de push.

- [ ] **Step 3: Push / deploy**

```bash
git push
```

Verificar en producción: `https://eiri-talleres.vercel.app/arena` redirige a login sin sesión; con sesión carga la arena. Nota: en Vercel el filesystem es efímero pero la DB es Turso (`TURSO_DATABASE_URL`) — los uploads van a S3, sin cambios necesarios.
